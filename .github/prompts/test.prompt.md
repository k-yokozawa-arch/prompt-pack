---
description: "テスト作成（ユニット/E2E）"
---

# 🧪 テスト作成

## テスト戦略

### ユニットテスト（Go）
```go
func TestXxx(t *testing.T) {
    // Arrange
    sut := NewService(mockDep)
    
    // Act
    result, err := sut.DoSomething(input)
    
    // Assert
    require.NoError(t, err)
    assert.Equal(t, expected, result)
}
```

### テーブル駆動テスト
```go
tests := []struct {
    name    string
    input   Input
    want    Output
    wantErr bool
}{
    {"正常系", validInput, expectedOutput, false},
    {"空入力", emptyInput, nil, true},
    {"境界値", boundaryInput, boundaryOutput, false},
}
for _, tt := range tests {
    t.Run(tt.name, func(t *testing.T) {
        // ...
    })
}
```

### フロントエンドテスト
- React Testing Library
- MSW（APIモック）

## テストケース設計

### 正常系
- 有効な入力 → 期待する出力
- 複数パターンの有効入力

### 異常系
- 無効な入力 → 適切なエラー
- 認可エラー
- 外部依存の失敗

### 境界値
- 最小値/最大値
- 空/null/undefined
- 長さ制限

### エッジケース
- 同時実行
- タイムアウト
- 部分的な成功

## モック方針
- DB: インメモリ実装 or モック
- 外部API: MSW / httptest
- 時間: fake timer

---

## 📝 テスト対象

**対象**: （関数/コンポーネント/エンドポイント）
**種別**: （ユニット/統合/E2E）
**重点ケース**: （特にテストしたいシナリオ）

---

上記に従ってテストコードを生成してください。
